const initState = {
    name: ""
}

const transcription = (state = initState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default transcription;